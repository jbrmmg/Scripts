#!/bin/bash
# Build Date:    ${script.build.date}
# Build Version: ${version}
#
# Blank script, does nothing
#
# ENVIRONMENT VARIABLES
#
# $Environment             = name of environment
#
echo Blank deploy version ${version} ${script.build.date}
